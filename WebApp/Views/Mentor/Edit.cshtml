@model WebApp.ViewModels.MentorViewModel

@{
    ViewData["Title"] = "Edit Mentor";
}

<h2>@ViewData["Title"]</h2>

@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-warning" role="alert">
        @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
        {
            <div>@error.ErrorMessage</div>
        }
    </div>
}

<form asp-action="Edit" method="post" enctype="multipart/form-data">
    <input type="hidden" asp-for="Id" />

    <div class="form-group">
        <label asp-for="Name"></label>
        <input asp-for="Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Surname"></label>
        <input asp-for="Surname" class="form-control" />
        <span asp-validation-for="Surname" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="TypeOfWorkId">Type of Work</label>
        <select asp-for="TypeOfWorkId" class="form-control" asp-items="@(ViewData["TypeOfWorkList"] as SelectList)"></select>
        <span asp-validation-for="TypeOfWorkId" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label for="AreaIds">Select Areas</label>
        <select asp-for="AreaIds" class="form-control" asp-items="ViewBag.AreaIds" multiple="multiple"></select>
    </div>

    <div class="form-group">
        <label for="ImageFile" class="control-label">Change Image</label>
        <input type="file" name="ImageFile" class="form-control" />
        @if (!string.IsNullOrEmpty(Model.ImagePath))
        {
            <p>Current image:</p>
            <img src="@Model.ImagePath" alt="Current image" style="max-height: 150px;" />
        }
    </div>


    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
